// Le preset jest-expo fournit déjà un transformIgnorePatterns adapté aux
// paquets React Native/Expo (voir node_modules/jest-expo) — ne pas le
// redéfinir ici, un override écraserait celui du preset au lieu de le
// compléter.
module.exports = {
  preset: "jest-expo",
};
