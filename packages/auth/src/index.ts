// Types et utilitaires d'authentification partagés (forme du payload JWT,
// contrat AsyncLocalStorage TenantContext, etc.) — pas de secrets ici.
// Contenu ajouté en Phase 2 (Authentification) et Phase 3 (Multi-tenancy).
export {};
